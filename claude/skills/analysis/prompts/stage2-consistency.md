Review the Stage 2 fan-out outputs for `{service}/{domain}`.

This is a post-writer consistency pass. Do not rewrite the Stage 2 files.
Return a concise Markdown note focused on cross-file consistency only.

Check for:
- endpoint to domain overview consistency
- endpoint to data model consistency
- external integration references matching the API/data narrative
- duplicate or contradictory terminology

## Stage 1 Memo
{stage1_memo}

## Combined Stage 2 Output
{combined_output}
